# Supabase — get from https://supabase.com/dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# NEVER expose this client-side. Service role bypasses all RLS — server-only.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# n8n webhook secrets — rotate if compromised
N8N_CHAT_SECRET=your-n8n-chat-secret
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id
N8N_MANAGER_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-manager-webhook-id

# Agent API shared secret (x-agent-secret header)
AGENT_SECRET=your-agent-secret

# Optional: override redirect URL in local development
# NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000/app/dashboard
